{
  "type": "object",
  "extends": {
    "type": "object",
    "javaType": "org.hawkular.cmdgw.api.AuthMessage"
  },
  "javaType": "org.hawkular.cmdgw.api.ExportJdrResponse",
  "description": "Results of the JDR export. The actual report file content must be appended to this JSON message.",
  "additionalProperties": false,
  "properties": {
    "resourcePath": {
      "description" : "The inventory path to the server that has executed the operation",
      "type": "string"
    },
    "status": {
      "description": "The success or failure flag.",
      "type": "string"
    },
    "message": {
      "description": "An additional message describing the results.",
      "type": "string"
    },
    "fileName": {
      "description": "The report's file name to be presented to the client, based on the name generated at the server.",
      "type": "string"
    }
  },
  "required": ["resourcePath", "status"]
}
